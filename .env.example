# Timesheet Invoice App - Docker Compose Environment

# Required secrets (set strong values in production)
SESSION_SECRET=change-me-please

# App credentials (either APP_PASSWORD or APP_PASSWORD_HASH)
APP_USERNAME=admin
APP_PASSWORD=admin
# APP_PASSWORD_HASH=

# Database location inside container (do not change unless needed)
DATABASE_PATH=/data/app.db

# Timezone
TZ=Pacific/Auckland
