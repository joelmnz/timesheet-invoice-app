# Timesheet Invoice App - Docker Compose Environment

# REQUIRED: Session secret (use a strong random value, at least 32 characters)
# Generate with: openssl rand -base64 32
# CRITICAL: Change this in production!
SESSION_SECRET=change-me-please

# REQUIRED: App credentials
# IMPORTANT: You MUST set APP_PASSWORD or APP_PASSWORD_HASH in production
# The default admin/admin will be rejected in production
APP_USERNAME=admin
# Option 1: Set plain password (will be hashed at startup)
APP_PASSWORD=admin
# Option 2: Pre-hashed password (RECOMMENDED - more secure)
# Generate with: docker run --rm oven/bun:latest bun -e "import bcrypt from 'bcrypt'; console.log(await bcrypt.hash('your-password', 12))"
# APP_PASSWORD_HASH=

# Database location inside container (do not change unless needed)
DATABASE_PATH=/data/app.db

# Timezone
TZ=Pacific/Auckland

# CORS configuration (only needed if frontend is on a different domain)
# Comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://timesheet.mydomain.com,https://app.example.com
# If not set, CORS is disabled (assumes same-origin deployment with frontend served by backend)
# ALLOWED_ORIGINS=

# Allow requests with no origin header (e.g., Postman) - defaults to false
# Only enable if needed for testing
# ALLOW_NO_ORIGIN=true
